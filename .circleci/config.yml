version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: npm i
      - run: npm run lint

# Working with Enviroment Variables

  test:
    docker: 
    - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: npm i
      - run: npm run test

  analyze:
      docker:
        - image: circleci/node:13.8.0
      steps:
        - checkout
        - run: npm i
        - run: npm audit

      
workflows:
  
  welcome:
    jobs:
      - bulid
      - test:
          requires:
            - build
      - analyze:
          requires:
            - test
      
